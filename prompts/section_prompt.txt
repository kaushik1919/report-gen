You are an academic writer. Write structured JSON content for ONE section of a university report.

Topic: $topic
Section Title: $title
Heading Level: $level
Target Word Count: $target_words
Instructions: $instructions
$context_hint

Return ONLY a JSON object matching this schema exactly. No markdown. No prose.

{
  "section_id": "$section_id",
  "title": "$title",
  "level": $level,
  "blocks": [
    {"type": "paragraph", "text": "Full paragraph text here — 100 to 200 words, formal register."},
    {"type": "bullets", "items": ["Concise point one.", "Concise point two.", "Concise point three."]},
    {"type": "table", "headers": ["Column 1", "Column 2"], "rows": [["value 1", "value 2"]]}
  ],
  "citations": []
}

Rules:
- Use academic tone, third person, formal register throughout
- Each paragraph block must be 100 to 200 words
- Do not repeat content already covered in previous sections
- Leave the citations array empty — do not hallucinate sources
- Do not use markdown syntax (no **, no ##, no -) inside text fields
- Use only block types: paragraph, bullets, table
- Return valid JSON only — no other text before or after
